# Architecture Decision: Run Final Build and Type-Check Verification

**Issue:** 9b0522de-6d83-4901-97c9-d8263c98f4ed  
**Branch:** feature/9b0522de-run-final-build-and-type-check-verification  
**Date:** 2026-06-14

---

## Problem Statement

This is a **verification-only** issue: confirm the entire project builds cleanly (`npm run build` exits 0) and passes TypeScript type-checking (`npm run type-check` exits 0). All feature work has merged — this issue integrates and validates.

---

## Pre-Flight Findings

### Node Modules
`node_modules/` is absent from the repo. The developer MUST run `npm install` first using the committed `package-lock.json` to reproduce the exact dependency tree.

### TypeScript Binary
`tsc` is not globally installed. `npm run type-check` invokes `tsc --noEmit`; it must be run via `./node_modules/.bin/tsc` or as `npm run type-check` (npm resolves `.bin` automatically). After `npm install` this will work correctly.

### Known Pre-existing Issues to Fix

1. **`text-muted-foreground` in `TeamMemberCard.tsx`** (line 32)  
   Tailwind v4 does not include ShadCN CSS variable tokens like `text-muted-foreground`. This class silently produces no style output, not a TypeScript error, but is a functional bug. Replace with an explicit Tailwind color utility (e.g., `text-gray-500`). This is consistent with the project decision documented in `.glori-builder/decisions.md` — ShadCN is not installed; use Tailwind utilities directly.

2. **`next-env.d.ts` missing**  
   This file is normally generated by `next build`/`next dev` and is gitignored. It will be created during the first `npm install` + `npm run build` run. The `tsconfig.json` includes it in the `include` array — this is normal Next.js behavior and is not an error.

3. **`GLORI-BUILDER.md` verification section** — already corrected by architect:  
   - Removed the broken `npm run test` step (no test runner is configured).  
   - Verification chain is now: lint → type-check → build.

4. **Generic layout metadata** — `src/app/layout.tsx` has `title: "App"` and `description: "App description"`. This is not a build/type-check blocker but should be updated to match the product (`"Quetrex Foundation"`) per MEMORY.md flag.

5. **`src/app/page.tsx` inline `companyData` stub** — the mock data module (`src/lib/data.ts`) from issue `e879c601` has NOT yet merged (status: `ai_pr_ready`). The home page uses an inline stub intentionally with a `TODO(QUE-e879c601)` comment. This is acceptable for final build verification — it compiles fine. **Do not remove the stub or add a missing import for `src/lib/data.ts`** since that file does not exist on this branch.

---

## Technical Approach

Single workstream: install dependencies, fix the `text-muted-foreground` class in `TeamMemberCard.tsx`, update layout metadata, then run and confirm both verification commands exit 0.

**No new pages, no new business logic, no auth surface.** This is a polish + integration-verification pass.

---

## File Ownership Map

### Workstream: `verification` (single developer, no parallel work)

| File | Action |
|------|--------|
| `src/components/TeamMemberCard.tsx` | Fix `text-muted-foreground` → `text-gray-500` |
| `src/app/layout.tsx` | Update metadata title/description to match Quetrex Foundation branding |
| `GLORI-BUILDER.md` | Already fixed by architect (verification chain corrected) |

**Test files:** No test files exist in this project; none required for this issue (no new business logic added).

---

## Dependency Order

1. Run `npm install` (required for all subsequent steps)
2. Fix `src/components/TeamMemberCard.tsx`
3. Fix `src/app/layout.tsx`
4. Run `npm run lint` — must exit 0
5. Run `npm run type-check` — must exit 0
6. Run `timeout 600 npm run build` — must exit 0
7. Commit all changes

---

## Acceptance Criteria

- `npm install` completes without errors using the committed `package-lock.json`
- `npm run lint` exits 0
- `npm run type-check` exits 0 (invokes `tsc --noEmit` via local node_modules)
- `timeout 600 npm run build` exits 0 (Next.js production build completes)
- `src/components/TeamMemberCard.tsx` uses `text-gray-500` instead of `text-muted-foreground`
- `src/app/layout.tsx` metadata reflects "Quetrex Foundation" branding
- No `npm run dev` or running server is invoked at any point

---

## Assumptions

- The sibling issues currently in `ai_pr_ready` status (mock data module, root layout, About page refactors, Fly.io config, etc.) have NOT yet merged to main and are NOT expected to be present on this branch. The developer must work only with what is committed on `main` as of this checkout.
- `src/lib/data.ts` does NOT exist; inline data stubs in `page.tsx` and `about/page.tsx` are correct and intentional for this branch.

---

## Designer Required

`designer_required: false` — this issue involves no UI design changes; it is a verification/polish pass only.
